language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-pattern-lab-patterns' ]; then cd .. && eval "mv $currentfolder generator-pattern-lab-patterns" && cd generator-pattern-lab-patterns; fi
